---
"@truefoundry/trueforge-core": patch
"@truefoundry/trueforge": patch
---

Add `user.tool_approval_policy` event schema (`allow_session`, optional expiry) to send-event items and turn stream / session event unions. Per-call allow remains `user.tool_approval`.
